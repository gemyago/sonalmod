---
phase: 01-acp-discovery-and-capability-map
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agent/acp-cli/go.mod
  - tests/agent/acp-cli/main.go
  - tests/agent/acp-cli/client.go
  - tests/agent/acp-cli/transcript.go
  - tests/agent/acp-cli/client_test.go
  - tests/agent/acp-cli/transcript_test.go
  - tests/agent/acp-cli/project.json
  - tests/AGENTS.md
autonomous: true
requirements: [CODE-01]
user_setup: []
must_haves:
  truths:
    - Sonalmod has a repo-local ACP probe CLI that can negotiate ACP capabilities over stdio and execute the minimum session lifecycle needed for OpenCode experiments.
    - The probe captures raw JSON-RPC requests, responses, and notifications to transcript files without relying on PTY scraping.
  artifacts:
    - tests/agent/acp-cli/main.go
    - tests/agent/acp-cli/client.go
    - tests/agent/acp-cli/client_test.go
    - tests/AGENTS.md
  key_links:
    - tests/agent/acp-cli/main.go uses tests/agent/acp-cli/client.go for ACP lifecycle handling.
    - tests/agent/acp-cli/transcript.go is exercised by tests/agent/acp-cli/transcript_test.go.
    - tests/AGENTS.md documents how to run the probe against a real ACP agent.
---

<objective>
Build a reusable ACP probe harness inside the repo for OpenCode experiments.

Purpose: Sonalmod needs real ACP evidence before it can design a stable OpenCode integration.
Output: A Go-based ACP probe CLI, transcript capture support, tests, and operator instructions.
</objective>

<execution_context>
@/Users/jenya/projects/sonalmod/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jenya/projects/sonalmod/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-acp-discovery-and-capability-map/01-CONTEXT.md
@.planning/phases/01-acp-discovery-and-capability-map/01-RESEARCH.md
@./AGENTS.md
@tests/AGENTS.md
@runtime/AGENTS.md
@docs/golang-coding-guide.md
@tests/agent/integration-cli/main.go
@tests/agent/integration-cli/cli.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the ACP probe CLI module and transport client</name>
  <files>tests/agent/acp-cli/go.mod, tests/agent/acp-cli/main.go, tests/agent/acp-cli/client.go, tests/agent/acp-cli/project.json</files>
  <read_first>AGENTS.md, tests/AGENTS.md, runtime/AGENTS.md, docs/golang-coding-guide.md, tests/agent/integration-cli/main.go, tests/agent/integration-cli/cli.go</read_first>
  <action>Create a new Go module at `tests/agent/acp-cli` that follows the same high-level CLI shape as `tests/agent/integration-cli`, but targets external ACP agents instead of Sonalmod's internal runner. Add a CLI entrypoint that accepts `--agent-command`, repeated `--agent-arg`, `--cwd`, `--prompt`, `--transcript`, `--load-session`, and `--cancel-after`. In `client.go`, implement newline-delimited JSON-RPC stdio transport that launches the downstream subprocess, sends `initialize`, reads the negotiated capabilities, runs `session/new` or `session/load` as appropriate, sends `session/prompt`, and optionally sends `session/cancel` after the requested delay. Treat capability-gated methods like `session/load` as conditional on the advertised capability flags instead of assuming support.</action>
  <verify>cd tests/agent/acp-cli && go test ./...</verify>
  <acceptance_criteria>
    - `tests/agent/acp-cli/main.go` contains flags named `agent-command`, `cwd`, `prompt`, `transcript`, `load-session`, and `cancel-after`
    - `tests/agent/acp-cli/client.go` contains JSON-RPC method strings `initialize`, `session/new`, `session/prompt`, and `session/cancel`
    - `tests/agent/acp-cli/client.go` checks agent capabilities before attempting `session/load`
    - `tests/agent/acp-cli/project.json` exists and defines a testable project entry for the new module
  </acceptance_criteria>
  <done>Probe CLI compiles, lifecycle code exists, and the module has a runnable `go test ./...` path.</done>
</task>

<task type="auto">
  <name>Task 2: Add transcript capture, tests, and run instructions</name>
  <files>tests/agent/acp-cli/transcript.go, tests/agent/acp-cli/client_test.go, tests/agent/acp-cli/transcript_test.go, tests/AGENTS.md</files>
  <read_first>AGENTS.md, tests/AGENTS.md, docs/golang-coding-guide.md, tests/agent/integration-cli/cli_test.go</read_first>
  <action>Add transcript recording that writes every outbound request and inbound response or notification as JSON lines to the file passed by `--transcript`. Cover the client with tests for newline framing, capability parsing, transcript writing, and cancellation request emission. Update `tests/AGENTS.md` with a short section for the ACP probe that explains the expected manual setup, the probe command shape, and the fact that the user must have `opencode` installed and authenticated before real ACP experiments run.</action>
  <verify>cd tests/agent/acp-cli && go test ./...</verify>
  <acceptance_criteria>
    - `tests/agent/acp-cli/transcript.go` contains logic to write both request and response envelopes
    - `tests/agent/acp-cli/client_test.go` contains a test covering capability-gated session loading
    - `tests/agent/acp-cli/transcript_test.go` exits successfully under `go test`
    - `tests/AGENTS.md` contains the string `ACP probe`
    - `tests/AGENTS.md` contains the string `opencode acp`
  </acceptance_criteria>
  <done>Probe transcripts are test-covered and operators have repo-local instructions for real ACP runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd tests/agent/acp-cli && go test ./...`
- [ ] `rg -n "ACP probe|opencode acp" tests/AGENTS.md`
- [ ] `rg -n "initialize|session/new|session/prompt|session/cancel" tests/agent/acp-cli/client.go`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Sonalmod has a reusable ACP probe harness ready for real OpenCode experiments
</success_criteria>

<output>
After completion, create `.planning/phases/01-acp-discovery-and-capability-map/01-01-SUMMARY.md`
</output>
